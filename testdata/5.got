package test

// Se is ...
type Se struct {
	// f is ...
	f float64

	// external
	r struct {
		// o - d
		o int
	}
}

// TestStruct is ...
type TestStruct struct {
	// seValue is ...
	seValue   Se
}
